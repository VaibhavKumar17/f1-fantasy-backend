import { Link } from "react-router-dom";
import { motion } from "framer-motion";
import { Flag, Users, Trophy, ChevronRight, Zap, Calendar } from "lucide-react";
import { Button } from "@/components/ui/button";
import { races2026 } from "@/data/drivers";

const Index = () => {
  const nextRace = races2026.find((r) => r.status === "upcoming");

  return (
    <div className="min-h-screen">
      {/* Hero */}
      <section className="relative flex min-h-[85vh] items-center overflow-hidden">
        {/* Background effects */}
        <div className="absolute inset-0 bg-carbon opacity-50" />
        <div className="absolute -right-40 -top-40 h-[600px] w-[600px] rounded-full bg-primary/5 blur-3xl" />
        <div className="absolute -bottom-20 -left-20 h-[400px] w-[400px] rounded-full bg-accent/5 blur-3xl" />

        <div className="container relative mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            className="max-w-3xl"
          >
            <div className="mb-4 inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-4 py-1.5">
              <Zap className="h-3.5 w-3.5 text-primary" />
              <span className="font-racing text-xs font-medium tracking-wider text-primary">
                SEASON 2025
              </span>
            </div>

            <h1 className="mb-6 font-racing text-5xl font-black leading-tight tracking-tight md:text-7xl">
              <span className="text-foreground">F1 FANTASY</span>
              <br />
              <span className="text-gradient-red">DELHI NCR</span>
            </h1>

            <p className="mb-8 max-w-lg text-lg text-muted-foreground">
              Build your dream team of 5 drivers. Compete against the Delhi NCR
              community. Climb the leaderboard every race weekend.
            </p>

            <div className="flex flex-wrap gap-4">
              <Button asChild size="lg" className="font-racing text-sm tracking-wider glow-red">
                <Link to="/my-team">
                  <Users className="mr-2 h-4 w-4" />
                  CREATE YOUR TEAM
                </Link>
              </Button>
              <Button asChild variant="outline" size="lg" className="font-racing text-sm tracking-wider">
                <Link to="/leaderboard">
                  <Trophy className="mr-2 h-4 w-4" />
                  LEADERBOARD
                </Link>
              </Button>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Next Race Banner */}
      {nextRace && (
        <section className="border-y border-border/50 bg-card/50">
          <div className="container mx-auto px-4 py-6">
            <motion.div
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              className="flex flex-col items-center justify-between gap-4 md:flex-row"
            >
              <div className="flex items-center gap-4">
                <div className="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-2xl">
                  {nextRace.flag}
                </div>
                <div>
                  <p className="text-xs font-medium uppercase tracking-widest text-muted-foreground">
                    Next Race
                  </p>
                  <p className="font-racing text-lg font-bold text-foreground">
                    {nextRace.name}
                  </p>
                </div>
              </div>
              <div className="flex items-center gap-6">
                <div className="text-center">
                  <p className="text-xs text-muted-foreground">Circuit</p>
                  <p className="text-sm font-medium text-foreground">{nextRace.circuit}</p>
                </div>
                <div className="text-center">
                  <p className="text-xs text-muted-foreground">Date</p>
                  <p className="text-sm font-medium text-foreground">
                    {new Date(nextRace.date).toLocaleDateString("en-US", {
                      month: "short",
                      day: "numeric",
                    })}
                  </p>
                </div>
                <Button asChild variant="ghost" size="sm">
                  <Link to="/schedule">
                    Full Schedule <ChevronRight className="ml-1 h-4 w-4" />
                  </Link>
                </Button>
              </div>
            </motion.div>
          </div>
        </section>
      )}

      {/* How it works */}
      <section className="py-24">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            className="mb-16 text-center"
          >
            <h2 className="mb-4 font-racing text-3xl font-bold text-foreground">
              HOW IT WORKS
            </h2>
            <p className="text-muted-foreground">
              Three steps to F1 glory in the Delhi NCR community
            </p>
          </motion.div>

          <div className="grid gap-8 md:grid-cols-3">
            {[
              {
                icon: Users,
                title: "Pick Your Drivers",
                desc: "Select 5 drivers from the 2025 grid within the budget cap. Choose wisely!",
                step: "01",
              },
              {
                icon: Flag,
                title: "Race Weekend",
                desc: "Your drivers earn points based on real F1 race results every weekend.",
                step: "02",
              },
              {
                icon: Trophy,
                title: "Climb the Board",
                desc: "Compare scores with the community. Top the leaderboard to claim bragging rights!",
                step: "03",
              },
            ].map((item, i) => (
              <motion.div
                key={item.step}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ delay: i * 0.15 }}
                className="group relative rounded-2xl border border-border/50 bg-card p-8 transition-all hover:border-primary/30 hover:glow-red"
              >
                <span className="absolute right-6 top-6 font-racing text-4xl font-black text-muted/50">
                  {item.step}
                </span>
                <div className="mb-4 flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-primary transition-colors group-hover:bg-primary group-hover:text-primary-foreground">
                  <item.icon className="h-6 w-6" />
                </div>
                <h3 className="mb-2 font-racing text-lg font-bold text-foreground">
                  {item.title}
                </h3>
                <p className="text-sm text-muted-foreground">{item.desc}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="border-t border-border/50 py-24">
        <div className="container mx-auto px-4 text-center">
          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
          >
            <Calendar className="mx-auto mb-4 h-10 w-10 text-primary" />
            <h2 className="mb-4 font-racing text-3xl font-bold text-foreground">
              READY TO RACE?
            </h2>
            <p className="mx-auto mb-8 max-w-md text-muted-foreground">
              Join the Delhi NCR F1 community and start building your fantasy team today.
            </p>
            <Button asChild size="lg" className="font-racing text-sm tracking-wider glow-red">
              <Link to="/my-team">
                GET STARTED <ChevronRight className="ml-2 h-4 w-4" />
              </Link>
            </Button>
          </motion.div>
        </div>
      </section>
    </div>
  );
};

export default Index;
